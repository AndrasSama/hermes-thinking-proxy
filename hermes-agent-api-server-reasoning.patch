diff --git a/gateway/platforms/api_server.py b/gateway/platforms/api_server.py
index 013bce571..ea9ed1582 100644
--- a/gateway/platforms/api_server.py
+++ b/gateway/platforms/api_server.py
@@ -1065,6 +1065,7 @@ class APIServerAdapter(BasePlatformAdapter):
         ephemeral_system_prompt: Optional[str] = None,
         session_id: Optional[str] = None,
         stream_delta_callback=None,
+        reasoning_callback=None,
         tool_progress_callback=None,
         tool_start_callback=None,
         tool_complete_callback=None,
@@ -1119,6 +1120,7 @@ class APIServerAdapter(BasePlatformAdapter):
             session_id=session_id,
             platform="api_server",
             stream_delta_callback=stream_delta_callback,
+            reasoning_callback=reasoning_callback,
             tool_progress_callback=tool_progress_callback,
             tool_start_callback=tool_start_callback,
             tool_complete_callback=tool_complete_callback,
@@ -1931,6 +1933,21 @@ class APIServerAdapter(BasePlatformAdapter):
             import queue as _q
             _stream_q: _q.Queue = _q.Queue()
 
+            # Accumulate reasoning content for injection into SSE deltas.
+            # Stored in closure so _write_sse_chat_completion can read it
+            # back when building chunk deltas.
+            _reasoning_parts: list[str] = []
+
+            def _on_reasoning(text: str) -> None:
+                if text:
+                    _reasoning_parts.append(text)
+
+            def _get_reasoning_text(reset: bool = False) -> str:
+                txt = "".join(_reasoning_parts)
+                if reset:
+                    _reasoning_parts.clear()
+                return txt
+
             def _on_delta(delta):
                 # Filter out None — the agent fires stream_delta_callback(None)
                 # to signal the CLI display to close its response box before
@@ -2005,6 +2022,7 @@ class APIServerAdapter(BasePlatformAdapter):
                 ephemeral_system_prompt=system_prompt,
                 session_id=session_id,
                 stream_delta_callback=_on_delta,
+                reasoning_callback=_on_reasoning,
                 tool_start_callback=_on_tool_start,
                 tool_complete_callback=_on_tool_complete,
                 agent_ref=agent_ref,
@@ -2018,6 +2036,7 @@ class APIServerAdapter(BasePlatformAdapter):
                 request, completion_id, model_name, created, _stream_q,
                 agent_task, agent_ref, session_id=session_id,
                 gateway_session_key=gateway_session_key,
+                get_reasoning_text=_get_reasoning_text,
             )
 
         # Non-streaming: run the agent (with optional Idempotency-Key)
@@ -2133,7 +2152,7 @@ class APIServerAdapter(BasePlatformAdapter):
     async def _write_sse_chat_completion(
         self, request: "web.Request", completion_id: str, model: str,
         created: int, stream_q, agent_task, agent_ref=None, session_id: str = None,
-        gateway_session_key: str = None,
+        gateway_session_key: str = None, get_reasoning_text=None,
     ) -> "web.StreamResponse":
         """Write real streaming SSE from agent's stream_delta_callback queue.
 
@@ -2190,6 +2209,21 @@ class APIServerAdapter(BasePlatformAdapter):
                         f"event: hermes.tool.progress\ndata: {event_data}\n\n".encode()
                     )
                 else:
+                    # Emit accumulated reasoning once as a <thinking> block
+                    # before the first content delta, so clients (Cline, etc.)
+                    # can render a separate thinking panel.
+                    if get_reasoning_text is not None:
+                        rtxt = get_reasoning_text()
+                        if rtxt:
+                            thinking_chunk = {
+                                "id": completion_id, "object": "chat.completion.chunk",
+                                "created": created, "model": model,
+                                "choices": [{"index": 0, "delta": {
+                                    "content": "<thinking>\n" + rtxt + "\n</thinking>\n\n",
+                                }, "finish_reason": None}],
+                            }
+                            await response.write(f"data: {json.dumps(thinking_chunk)}\n\n".encode())
+                            get_reasoning_text(reset=True)  # only emit once
                     content_chunk = {
                         "id": completion_id, "object": "chat.completion.chunk",
                         "created": created, "model": model,
@@ -3704,6 +3738,7 @@ class APIServerAdapter(BasePlatformAdapter):
         ephemeral_system_prompt: Optional[str] = None,
         session_id: Optional[str] = None,
         stream_delta_callback=None,
+        reasoning_callback=None,
         tool_progress_callback=None,
         tool_start_callback=None,
         tool_complete_callback=None,
@@ -3736,6 +3771,7 @@ class APIServerAdapter(BasePlatformBase):
                     ephemeral_system_prompt=ephemeral_system_prompt,
                     session_id=session_id,
                     stream_delta_callback=stream_delta_callback,
+                    reasoning_callback=reasoning_callback,
                     tool_progress_callback=tool_progress_callback,
                     tool_start_callback=tool_start_callback,
                     tool_complete_callback=tool_complete_callback,